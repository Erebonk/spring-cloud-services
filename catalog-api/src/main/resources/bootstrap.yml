eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    fetchRegistry: true
    register-with-eureka: true
  instance:
    appname: catalog-gateway
spring:
  application:
    name: catalog-gateway

ribbon:
  restclient:
    enabled: true
